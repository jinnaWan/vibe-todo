# Backend ECS Runner Configuration
# AWS ECS Configuration
ECS_CLUSTER=github-runners-cluster
TASK_DEFINITION=github-runner-task
CAPACITY_PROVIDER=EC2
CONTAINER_NAME=github-runner

# Resource Configuration - Higher resources for .NET builds
CONTAINER_MEMORY=4096
CONTAINER_CPU=2048

# Network Configuration (optional - will use default VPC if not specified)
SECURITY_GROUPS=
SUBNETS=

# Runner Configuration
RUNNER_LABELS=self-hosted,aws-ecs,linux,arm64,dotnet,backend 